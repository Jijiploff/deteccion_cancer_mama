
Los sistemas deberán ser desplegados en Render para el Bakend y Vercel para el Frontend preferentemente, como control de versiones el codigo alojado en Github y para la documentación Jira. Para temas de redes neuronales deberán tener el siguiente secuencia: 1. EDA (limpieza de datos y estadísticos descriptivos), 2. Entrenamiento (tres modelos cásicos y dos híbridos), debe reportar tablas comparativos de resultados de los modelos entre estadísticos, tiempo de proceso, etc., matriz de confusión, mapa de calor, curva ROC, etc., debe grabarse el mejor modelo como .h para consumirlo y no estar reentrenando, 3. Validación cruzada (cross validation con 5 folks configurable), 4. Hiperparámetros (tunning), 5. Pruebas Estadísticas (validación de los modelos con pruebas estadísticas robustas), 6. Reportes (PDF, Word y Excel), deberá reportar tablas y figuras con su interpretación, lo mismo para las salidas a pantalla. Asi mismo un dashboard al cargar depues de validar las credenciales.
elaborar el manual de instalación, manual de usuario, diagramas en Mermaid de arquitectura, modelo de datos, diagrama de componentes, así mismo los principales diagramas de secuencia y de estados. 